// Variables:
var altura = 100; //Si altura es > a 52 puede pasar.
var edad = 11; //Si la edad es > 10 años puede pasar.
    if (altura > 52 ){ 
        console.log("¡Súbete, chico!")
        if (edad >= 11){
            console.log("¡Tienes la edad y la altura necesaria!")
        }

    }

    else if (altura < 52 )
        console.log("Lo siento, chico. Tal vez el próximo año")
        else if (edad < 11)
        console.log("¡No cumples ni con la edad ni con la estatura necesaria!")
